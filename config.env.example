#===============================================================================
# Eigendaten Office Cloud Suite - Beispiel-Konfiguration
#
# Diese Datei zeigt alle verfuegbaren Parameter.
#
# Primaere Verwendung: Parameter direkt per curl uebergeben:
#
#   curl -sSL https://raw.githubusercontent.com/percyengler/eigendaten-installer/main/install.sh | \
#     sudo DOMAIN=demo-firma.de SERVER_ROLE=app ADMIN_EMAIL=admin@demo-firma.de bash
#
# Alternative: Config-Datei verwenden:
#   cp config.env.example config.env
#   nano config.env
#   source config.env && curl -sSL ... | sudo -E bash
#
#===============================================================================

#-------------------------------------------------------------------------------
# ERFORDERLICH
#-------------------------------------------------------------------------------

# Haupt-Domain (ohne www)
DOMAIN="demo-firma.de"

# Admin E-Mail (fuer Let's Encrypt Zertifikate)
ADMIN_EMAIL="admin@demo-firma.de"

# Server-Rolle:
#   app  = App-Server (Nextcloud, Paperless, Keycloak, Vaultwarden)
#   mail = Mail-Server (Mailcow)
#   full = Alles auf einem Server (nur fuer kleine Setups/Tests)
SERVER_ROLE="app"

#-------------------------------------------------------------------------------
# OPTIONAL: Benutzer
#-------------------------------------------------------------------------------

# Benutzer die angelegt werden sollen
# Format: vorname:nachname:email
# Mehrere Benutzer mit Komma trennen
#
# Beispiel:
# USERS="max:mustermann:m.mustermann@demo-firma.de,anna:schmidt:a.schmidt@demo-firma.de"
USERS=""

#-------------------------------------------------------------------------------
# OPTIONAL: Erweiterte Einstellungen
#-------------------------------------------------------------------------------

# SSH Port (Standard: 22)
# SSH_PORT="22"

# Zeitzone
# TIMEZONE="Europe/Berlin"

# Keine interaktiven Abfragen (fuer vollautomatische Installation)
# AUTO_CONFIRM="false"
